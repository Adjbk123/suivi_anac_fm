{% extends 'base_auth.html.twig' %}

{% block title %}ANAC BENIN | Email envoyé{% endblock %}

{% block auth_content %}
	<!-- Logo centré -->
	<div class="text-center mb-4">
		<img src="{{ asset('assets/images/logo_sf.png') }}" alt="ANAC BENIN" height="50" style="object-fit: contain;">
	</div>
	
	<div class="text-center">
		<h1 class="text-center">Email envoyé</h1>
		<div class="text-muted text-center mb-4">
			Si un compte existe avec cette adresse email, un email contenant un lien de réinitialisation vient d'être envoyé.
		</div>
		
		<div class="alert alert-info" role="alert">
			<p class="mb-0">
				Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
			</p>
		</div>
		
		<div class="text-muted text-center mb-4">
			Si vous n'avez pas reçu d'email, vérifiez votre dossier spam ou <a href="{{ path('app_check_email') }}">réessayez</a>.
		</div>
		
		<div class="text-center">
			<a href="{{ path('app_login') }}" class="btn btn-theme btn-lg d-block w-100 fw-500 mb-3">Retour à la connexion</a>
		</div>
	</div>
{% endblock %}
